# dynamic_form_resume_poc

Run:

1. npm install
2. npm start

Notes:

- Run on localhost:3000 and allow camera permissions.
- Camera opens only when user clicks "Take Selfie".
- Preview appears immediately after capture.
- Resume journey: copy/paste userId from URL to resume later.
